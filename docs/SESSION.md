# SESSION — GALTON-TIMER

現在の作業コンテキスト。常に最新状態に上書き。

---

## 現在のステータス

- **フェーズ**: 開発環境の基盤構築（GitHub 連携）→ 物理エンジン改善
- **前回の成果 (2/13)**: 運用ルール策定完了。コード監査により物理・UX の致命的課題（P0〜P3）を特定済み。
- **本日 (2/14)**: GitHub 連携のセットアップを実行中。

## 特定済みの課題（優先度順）

| ID | 優先度 | 概要 |
|----|--------|------|
| A1 | P0 | 垂直ドラッグの欠如による速度の不適正 |
| A2 | P0 | テレポート衝突検知（幾何学的衝突検知が必要） |
| A3 | P1 | レールを走るような不自然な X 補正 |
| A4 | P1 | 地面でのバウンド演出の欠如 |

## TODO

1. **GitHub 連携の実行 (Must)** ← 今ここ
   - [x] GitHub 上で `galton-timer` リポジトリを作成
   - [ ] ローカルで `git init` および `.gitignore` を設定
   - [ ] 初回コミットを行い、リモートへ push
2. **P0 物理課題の優先修正**
   - [ ] 垂直ドラッグの導入による速度の適正化 (A1)
   - [ ] 幾何学的なペグ衝突検知の実装（テレポートの廃止）(A2)
3. **基本挙動の磨き上げ**
   - [ ] 粒子がレールを走るような不自然な X 補正 (A3) の調整
   - [ ] 地面でのバウンド演出 (A4) の追加

## 開発メモ

- セーブポイント（GitHub push）ができるまでは、大きなコード変更は行わないこと。
- DUB/NEON テーマの実装は、基本挙動の磨き上げが満足いくレベルに達してから開始する。
- ドキュメント運用: `CHANGELOG.md`（事実の蓄積・追記型）/ `SESSION.md`（記憶の同期・上書き型）
